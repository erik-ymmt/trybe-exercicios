//bonus, using only one 'if'
const num1 = 12;
const num2 = 4;
const num3 = 8;

//check if any of the 3 numbers is uneven
if ((num1 % 2) != 0 || (num2 % 2) != 0 || (num3 % 2) != 0) {
    console.log('true. There is an uneven number.');
} else {
    console.log('false. They are all even.');
}